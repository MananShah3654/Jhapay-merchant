# Anthropic API key used by the /api/ai/chat serverless function.
# Create one at https://console.anthropic.com/ and set it in
# Vercel → Project → Settings → Environment Variables (do NOT commit a real key).
ANTHROPIC_API_KEY=
